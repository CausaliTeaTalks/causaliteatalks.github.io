source "https://rubygems.org"

gem "jekyll", "~> 4.3.0"   # Ensure you use the version compatible with your theme
gem "webrick"              # Required for Jekyll on some systems
gem "jekyll-seo-tag"

# If your theme is a remote theme (GitHub Pages compatible), include:
gem "jekyll-remote-theme"

# If your theme requires specific plugins, add them here.
# Example:
# gem "jekyll-paginate"
# gem "jekyll-seo-tag"
